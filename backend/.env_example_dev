# =================================
# 🚀 CONFIGURACIÓN DE DESARROLLO
# =================================

# Puertos de los servicios
API_PORT=8081
WS_PORT=8082
PROXY_PORT=8080

# Base de datos (Development)
DATABASE_DSN=postgres://user:password@localhost:5432/backend_dev?sslmode=disable

# JWT Configuration
JWT_SECRET=tu-super-secreto-jwt-para-desarrollo-local-muy-largo-y-seguro
JWT_EXPIRES_IN=24h

# Google Cloud Storage (opcional para desarrollo)
GCS_BUCKET_NAME=
GCS_SERVICE_ACCOUNT_KEY_PATH=

# Logging Level (debug, info, warn, error)
LOG_LEVEL=debug

# CORS Settings (manejado por el proxy)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:3000
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS,PATCH
CORS_ALLOWED_HEADERS=*
CORS_ALLOW_CREDENTIALS=true

# Environment
ENVIRONMENT=development

# =================================
# 📝 INSTRUCCIONES:
# =================================
# 1. Copia este archivo: cp .env_example_dev .env
# 2. Modifica los valores según tu configuración local
# 3. Para producción, usa valores más seguros
# 4. Nunca subas el archivo .env al repositorio 